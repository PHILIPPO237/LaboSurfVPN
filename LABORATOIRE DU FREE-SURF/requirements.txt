fastapi
uvicorn
pydantic
jinja2
itsdangerous
aiofiles
aiosqlite
python-multipart
aiohttp
httpx
python-dotenv
freezegun
passlib[bcrypt]
# bcrypt>=4.1 a retire l'attribut __about__ dont passlib 1.7.4 (derniere version,
# non maintenue) a besoin pour detecter le backend -> hachage des mots de passe
# echoue silencieusement (exception avalee), symptome : password_hash reste
# toujours vide meme apres reinitialisation. Verrouille ici pour que
# `pip install -r requirements.txt` n'installe plus jamais une version cassee.
bcrypt<4.1
curl_cffi
