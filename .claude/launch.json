{
  "version": "0.0.1",
  "configurations": [
    {
      "name": "labosurf-ui",
      "runtimeExecutable": "python",
      "runtimeArgs": [
        "-c",
        "import http.server as h, functools\nclass H(h.SimpleHTTPRequestHandler):\n    def end_headers(self):\n        self.send_header('Cache-Control', 'no-store')\n        super().end_headers()\nh.test(HandlerClass=functools.partial(H, directory='app/src/main/assets/www'), port=5173, bind='127.0.0.1')"
      ],
      "port": 5173
    }
  ]
}
