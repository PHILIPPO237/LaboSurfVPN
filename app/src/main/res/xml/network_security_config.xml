<?xml version="1.0" encoding="utf-8"?>
<!--
  Production : AUCUN trafic en clair. Tout passe par HTTPS avec les autorites de certification du systeme
  (le certificat du panel est verifie ; il n'existe pas de repli HTTP).
  Le developpement en local (panel sur 127.0.0.1 / 10.0.2.2) est autorise UNIQUEMENT dans les versions debug :
  voir app/src/debug/res/xml/network_security_config.xml.
-->
<network-security-config>
    <base-config cleartextTrafficPermitted="false" />
</network-security-config>
